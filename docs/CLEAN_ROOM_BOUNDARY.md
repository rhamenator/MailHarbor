# Clean-room boundary

Mail Harbor is a new implementation based on current public Chrome Extension and
Gmail API documentation. No historical source code, OAuth client files, tokens,
mailbox content, organization names, label rules, screenshots, or user data are
included.

All messages, senders, domains, IDs, and classifications in mock mode and tests
are fictional. Existing credential and archive files are never opened.
